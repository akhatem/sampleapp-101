<%= link_to (image_tag current_user.image.url.to_s, size: "90x90"), current_user %>
<h1 style="width: 290px"><%= current_user.name%></h1>
<span><%= pluralize(current_user.microposts.count, "micropost") %></span>
<span><%= link_to "view my profile", current_user %></span>
